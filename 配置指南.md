# Hexo 博客快速配置指南

## 🎨 个性化配置

### 1. 修改基本信息

编辑 `_config.yml`：

```yaml
# 网站基本信息
title: Over's Blog              # 网站标题
subtitle: 技术 · 生活 · 思考    # 副标题
description: 全栈开发者的数字花园  # 网站描述
author: Over                    # 作者名
```

### 2. 配置 Butterfly 主题

编辑 `_config.butterfly.yml`：

#### 网站图标和头像

```yaml
# Favicon（网站图标）
favicon: /img/favicon.png

# 头像
avatar:
  img: /img/avatar.jpg
  effect: true  # 头像会旋转
```

#### 导航菜单

```yaml
menu:
  首页: / || fas fa-home
  归档: /archives/ || fas fa-archive
  标签: /tags/ || fas fa-tags
  分类: /categories/ || fas fa-folder-open
  关于: /about/ || fas fa-heart
```

#### 社交链接

```yaml
social:
  fab fa-github: https://github.com/yourusername || GitHub
  fas fa-envelope: mailto:your@email.com || Email
  fab fa-twitter: https://twitter.com/yourusername || Twitter
```

#### 首页顶部图

```yaml
# 首页顶部大图
index_img: https://images.unsplash.com/photo-1501785888041-af3ef285b470

# 文章默认封面
default_top_img: https://images.unsplash.com/photo-1506744038136-46273834b3fb
```

#### 代码高亮主题

```yaml
highlight_theme: mac  # darker / pale night / light / ocean / mac / mac light
```

### 3. 创建必要页面

```bash
# 创建关于页面
npx hexo new page about

# 创建标签页面
npx hexo new page tags

# 创建分类页面
npx hexo new page categories
```

然后编辑生成的页面，添加 `type` 字段：

```yaml
# source/tags/index.md
---
title: 标签
date: 2026-01-29
type: "tags"
---

# source/categories/index.md
---
title: 分类
date: 2026-01-29
type: "categories"
---
```

## ✍️ 开始写作

### 创建新文章

```bash
npx hexo new "Next.js 14 新特性解析"
```

文章会生成在 `source/_posts/` 目录下。

### 文章模板

```markdown
---
title: Next.js 14 新特性解析
date: 2026-01-29 20:30:00
tags:
  - Next.js
  - React
  - Web开发
categories:
  - 技术
cover: https://images.unsplash.com/photo-1555066931-4365d14bab8c
description: 深入探索 Next.js 14 带来的革命性变化
top: false  # 是否置顶
---

## 摘要

这里是文章摘要...

<!-- more -->

## 正文

这里是文章正文...
```

### 插入图片

```markdown
# 方式1：使用外链
![图片描述](https://example.com/image.jpg)

# 方式2：使用本地图片
# 先在 source 目录下创建 images 文件夹
# 然后将图片放入，使用相对路径引用
![图片描述](/images/photo.jpg)
```

### 文章摘要分隔

使用 `<!-- more -->` 标记，之前的内容会在首页显示为摘要。

## 🚀 推荐插件

### 搜索功能

```bash
npm install hexo-generator-search --save
```

在 `_config.butterfly.yml` 中启用：

```yaml
local_search:
  enable: true
```

### RSS 订阅

```bash
npm install hexo-generator-feed --save
```

在 `_config.yml` 中添加：

```yaml
feed:
  type: atom
  path: atom.xml
  limit: 20
```

### 站点地图

```bash
npm install hexo-generator-sitemap --save
```

## 🎯 主题美化建议

### 1. 自定义颜色

在 `_config.butterfly.yml` 中：

```yaml
theme_color:
  enable: true
  main: "#49B1F5"
  paginator: "#00c4b6"
  button_hover: "#FF7242"
  text_selection: "#00c4b6"
  link_color: "#99a9bf"
  hr_color: "#A4D8FA"
```

### 2. 添加 Pjax（无刷新加载）

```yaml
pjax:
  enable: true
```

### 3. 开启阅读模式

```yaml
readmode: true
```

### 4. 添加加载动画

```yaml
preloader: true
```

## 📝 写作技巧

1. **文章分类**：使用分类来组织内容（技术、生活、思考等）
2. **添加标签**：为文章添加相关标签，方便检索
3. **封面图片**：使用高质量的封面图提升视觉效果
4. **摘要优化**：写好摘要，吸引读者点击
5. **定期更新**：保持更新频率，积累内容

## 🌐 部署到线上

### Vercel（推荐，最简单）

1. 将代码推送到 GitHub
2. 访问 [Vercel](https://vercel.com)
3. 导入 GitHub 仓库
4. 设置构建命令：`npx hexo generate`
5. 设置输出目录：`public`
6. 点击部署

### GitHub Pages

```yaml
# _config.yml
deploy:
  type: git
  repo: https://github.com/username/username.github.io
  branch: main
```

```bash
npm install hexo-deployer-git --save
npx hexo deploy
```

## 💡 常用命令速查

```bash
# 创建文章
npx hexo new "标题"

# 创建草稿
npx hexo new draft "标题"

# 发布草稿
npx hexo publish "标题"

# 清理 + 生成 + 启动
npx hexo clean && npx hexo g && npx hexo s

# 简写版本
npx hexo cl && npx hexo g && npx hexo s
```

---

🎉 现在可以开始写作了！访问 http://localhost:4000 查看你的博客。
